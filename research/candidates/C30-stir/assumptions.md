# C30 assumptions

- The authoritative source is Plonky3 `3152b14a89067c83775a8076cc262ffc48a1fd7c`; the focused runner rejects a different commit or changed PCS/workspace source.
- The exact compiled `Pcs::ZK` constant is authoritative for the adapter and must remain `false`.
- C20 and C30 use the same BabyBear field, quartic challenge extension, 4,096 public values generated by the same formula, and one opening point. Protocol-specific folding and hiding overhead remain different.
- The proxy is a polynomial-opening geometry match only. It is not a faithful AIR proxy, security-matched finalist comparison, or PQTC relation measurement.
- Postcard length is a native Rust proof payload length, not ABI calldata or a canonical PQTC codec.
- Timings are single-run wall-clock observations, not timing benchmarks. Peak RSS is the process-wide `getrusage` maximum.
- Because hiding is false, the run cannot become a privacy finalist regardless of proof size.
- Frozen v0.3/H0 remains the only faithful relation control; no SP-10 compression candidate was accepted.
