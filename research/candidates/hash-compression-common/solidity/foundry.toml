[profile.default]
src = "src"
test = "test"
out = "out"
cache_path = "cache"
solc_version = "0.8.30"
evm_version = "prague"
optimizer = true
optimizer_runs = 200
via_ir = true
libs = ["../../../../lib"]
fs_permissions = [
  { access = "read", path = "../vectors" },
  { access = "read-write", path = "../gas" },
]
