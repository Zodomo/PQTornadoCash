# SP-31 combined verifier optimization package

## Status

BENCHMARK_ONLY. No custody integration is authorized.

## Decision

Run V1-V9 from one fail-closed command. Keep measured isolated kernels and explicit component projections in separate JSON fields. Do not add overlapping microbenchmarks. V3+V4 is the only predeclared simplifying combination. A complete-transaction gate remains pending because the frozen canonical proof fixture and an isolated full-verifier variant are unavailable.
